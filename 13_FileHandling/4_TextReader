using System;
using System.IO;
class prog
{
    public static void Main(string[] args)
    {
        using (TextReader tr = File.OpenText("a.txt"))
        {
           Console.WriteLine(tr.ReadToEnd()); // to read full content of file
           Console.WriteLine(tr.ReadLine()); // to read Single Line
        }
       
    }
}
